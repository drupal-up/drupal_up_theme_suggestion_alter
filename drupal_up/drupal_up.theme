<?php

/**
 * Implements hook_theme_suggestions_alter().
 */
function drupal_up_theme_suggestions_alter(array &$suggestions, array $variables, $hook) {
  if ($hook == 'node') {
    $suggestions[] = 'node__drupalup';
  }
}
